apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: student-tracker-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: student.3.95.208.167.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tracker-service
            port:
              number: 8000
